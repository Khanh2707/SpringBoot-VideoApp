server:
  port: 8080

spring:
  application:
    name: VideoApp
  datasource:
    url: jdbc:mysql://localhost:3307/videoapp
    username: root
    password: root
    jpa:
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQLDialect
      hibernate:
        ddl-auto: update
      show-sql: true
